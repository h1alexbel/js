{
  "name": "01-v8",
  "version": "1.0.0",
  "dependencies": {
    "jest": "^29.6.1"
  },
  "type": "module",
  "scripts": {
    "ci": "npm install && npm run test && npm run cov",
    "test": "jest",
    "cov": "jest --coverage"
  },
  "jest": {
    "transform": {
      "^.+\\.jsx?$": "babel-jest"
    },
    "coverageThreshold": {
      "global": {
        "branches": 50,
        "functions": 50,
        "lines": 50,
        "statements": 50
      }
    }
  }
}
